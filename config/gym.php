<?php

/**
 * ⚙️ إعدادات عامة خاصة بالنادي.
 * الهدف من هذا الملف تجميع الأرقام والقيم التي كانت مكتوبة مباشرة
 * داخل الكنترولرات (Hardcoded)، حتى تُعدَّل من مكان واحد بلا المساس
 * بالكود، ولتكون نقطة الانطلاق عند بناء نظام الورديات لاحقاً.
 */

return [

    // 🕒 الساعة التي بعدها يُعتبر دوام الموظف "متأخر" عند تسجيل الحضور اليومي.
    // ملاحظة: هذا رقم عام لكل الموظفين مؤقتاً، وسيُستبدل مستقبلاً بمقارنة
    // كل موظف مع وردية العمل الخاصة به عند إضافة جدول shifts.
    'late_after_hour' => env('GYM_LATE_AFTER_HOUR', 9),

];